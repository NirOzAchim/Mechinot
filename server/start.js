/* ============================================================
   הרצה בייצור
   ⚠ `NODE_ENV=production node ...` בשורת ה-script **אינו עובד
     ב-cmd.exe** — ווינדוס אינו מכיר את התחביר הזה, והפקודה
     נכשלת בהודעה שאינה מסבירה דבר. קובץ קטן פותר את זה בלי
     תלות חיצונית (cross-env).
   ============================================================ */
process.env.NODE_ENV = "production";
await import("./index.js");
